# 20260516_115704 scratch obs=31 gripper roll alignment result

## Run

- script: `scripts/train_stage4_relaxed_gate_scratch_128_500.sh`
- run: `2026-05-16_11-57-04`
- resume: **없음 (scratch)**
- selected checkpoint: `model_499.pt`
- 관련 plan: `notes/20260516_113500_gripper_roll_alignment_plan.md`
- 관련 이전 결과: `notes/20260516_113838_gripper_roll_alignment_result.md`

## 이번 실험의 배경

이전 실험(`11-38-38`)에서 `--resume`으로 28-dim 체크포인트를 로드하면 RSL-RL이 저장된 네트워크 구조를 그대로 사용해 obs=31 변경이 무시됨을 확인.
또한 실행 파일이 `mt4_isaac_lab_task/source/`가 아니라 IsaacLab 내부 설치본(`isaaclab_tasks/direct/mt4_reach/`)임을 확인하고, 두 파일을 동기화 후 재실행.

## 코드/환경 수정 사항

- `IsaacLab/source/isaaclab_tasks/isaaclab_tasks/direct/mt4_reach/mt4_reach_env.py` ← 레포 파일과 동기화
- pyc 캐시 삭제 후 재실행
- `--resume` 제거 → RSL-RL이 env의 `observation_space=31`로 네트워크 새로 초기화

## 네트워크 구조 확인

```
Actor Model: Linear(in_features=31, out_features=64, bias=True)  ✅
```

obs=31(gripper_side 3값 포함)이 처음으로 온전히 적용됨.

## Main Metrics (최종 iteration 기준)

| 지표 | 값 | 비고 |
|------|-----|------|
| `success_rate` | ~0.002 | scratch 치고 양호 |
| `moving_pregrasp_final_rate` | ~0.51-0.54 | resumed 대비 낮음(scratch 특성) |
| `stage3_latched_rate` | ~0.85-0.86 | 양호 |
| `stage4_center_ready_rate` | ~0.002 | 출현 시작 |
| `mean_distance` | ~0.112-0.116 | resumed 대비 높음(scratch 특성) |
| `mean_insertion_alignment` | ~0.72-0.73 | 정렬 학습 진행 중 |
| **`mean_gripper_roll_alignment`** | **~0.000** | ✅ 처음으로 로그에 표시됨 |

## Interpretation

obs=31 네트워크가 정상 동작했다. `mean_gripper_roll_alignment`이 처음으로 로그에 출력되어 reward 및 observation이 모두 온전히 연결됨을 확인.

roll alignment 값이 ~0에 머문 것은 수렴 부족 때문이다. scratch 학습 500 iteration은 기본 alignment/reach를 배우기에도 빠듯하며, roll 제어까지 수렴하기엔 충분하지 않다. resumed 실험에서는 이미 학습된 가중치가 있어 빠르게 roll reward를 활용했지만, scratch에서는 기본 동작부터 배워야 한다.

`moving_pregrasp_final_rate`가 0.51인 것도 같은 이유다. 이전 resumed 실험(0.75-0.90)에 비해 낮지만, 순수 scratch 기준으로는 준수한 수준이다.

## 추가 확인: Isaac Sim cleanup hang 현상

학습 완료(264초) 후 Isaac Sim 프로세스가 30분 이상 CPU 104%로 종료되지 않는 현상 발생.
이전 `play` 세션이 남긴 kvdb lock과 충돌한 것으로 추정("Disabling key-value database because another kit process is locking it").
학습 체크포인트는 정상 저장됨(model_499.pt 확인). 이후 실험 시작 전 kill_isaac_processes.sh 실행 권장.

## Recommendation

obs=31 scratch 학습이 제대로 동작함을 확인했다. 다음 단계:

1. **longer scratch**: 동일 조건으로 1000-1500 iteration → roll alignment가 +0.5 이상으로 수렴하는지 확인
2. **resumed fine-tune**: 현재 `model_499.pt`에서 `--resume`으로 추가 학습 (obs=31 네트워크 그대로 유지)
3. **GUI 확인**: 현재 체크포인트로 play해 집게 bias가 개선됐는지 육안 확인
