# 20260516_110906 relaxed gate step_radius=0.080 result

## Run

- script: `scripts/train_stage4_relaxed_gate_128_300.sh`
- run: `2026-05-16_11-09-06`
- resume checkpoint: `2026-05-16_11-02-58/model_2399.pt`
- selected checkpoint: `model_2698.pt` (latest)

## Changes from previous run

| 파라미터 | 이전 | 이번 |
|---------|------|------|
| `step_radius` | 0.045m | **0.080m** |
| `hold_steps` | 2 | 2 |
| `reward_weight` | 20.0 | 20.0 |

## Main Metrics (최종 iteration 기준)

- `success_rate ≈ 0.004`
- `moving_pregrasp_final_rate ≈ 0.75` (이전: 0.0002)
- `moving_pregrasp_step_ready_rate ≈ 0.68` (이전: 0.002)
- `mean_moving_pregrasp_fraction ≈ 0.78`
- `stage3_latched_rate ≈ 0.78`
- `stage4_center_ready_rate ≈ 0.002`
- `mean_distance ≈ 0.067`
- `mean_target_contact_penalty = 0.0`

## Interpretation

`step_radius=0.080m`이 핵심 수정이었다. `mean_pregrasp_distance=0.075m` 근처에서 에이전트가 안정적으로 머물고 있었으므로, 0.035/0.045m gate는 사실상 닫혀 있었다. 0.080m로 올리자마자 gate가 68% 열렸고 `moving_pregrasp_final_rate`가 0.0002 → 0.75로 3900배 향상.

`stage4_center_ready_rate`도 0.002 수준으로 처음 등장했다. 파란 가이드가 최종 위치(빨간 중심)까지 도달하는 에피소드가 생기기 시작했다.

다만 300 iteration 만으로는 최종 삽입 정밀도 학습이 부족하다. `mean_best_target_center_distance ≈ 0.27m`로 아직 크다.

## Recommendation

동일 조건으로 학습 iteration을 늘리거나(800-1000), 또는 다음 단계로:
- `step_radius=0.080m` 유지
- `hold_steps=2` 유지
- `reward_weight`를 24-28로 소폭 상향해 최종 삽입 신호를 강화
- 500-800 iteration으로 재실험
